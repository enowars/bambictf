---
  - hosts: all
    become: yes
    become_method: sudo
    vars_files:
      - config_bambi.yml
      - static.yml

    roles:
      - base
      - filebeat
      - journalbeat
      - metricbeat
      - bambi-wireguard-router
      - router_trafficcapture
      - bambi-arkime
