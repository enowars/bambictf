---
  - hosts: all
    become: yes
    become_method: sudo
    vars_files:
      - config_bambi.yml

    roles:
      - role: filebeat
        vars:
          elk: {{ elk_adress }}
      - role: journalbeat
        vars:
          elk: {{ elk_adress }}
      - role: metricbeat
        vars:
          elk: {{ elk_adress }}
      - bambi-ssh-keys
      - firewall
      - bambi-wireguard-router
      - role: programs
        vars:
          program_list: "tmux"
