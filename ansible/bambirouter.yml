---
  - hosts: all
    become: yes
    become_method: sudo
    vars_files:
      - config_bambi.yml

    roles:
      - role: filebeat
        vars:
          elk: 192.168.1.0
      - role: journalbeat
        vars:
          elk: 192.168.1.0
      - role: metricbeat
        vars:
          elk: 192.168.1.0
      - bambi-ssh-keys
      - firewall
      - bambi-wireguard-router
      - tcpdump-moloch
      - role: programs
        vars:
          program_list: "tmux"
