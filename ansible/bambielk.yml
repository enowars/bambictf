---
  - hosts: all
    become: yes
    become_method: sudo
    vars_files:
      - config_bambi.yml
      - static.yml

    roles:
      - base
      - enoelk
      - docker-block-external
      - role: wireguard_configs
        vars:
          config_dir: elk
      - filebeat
      - journalbeat
      - metricbeat
