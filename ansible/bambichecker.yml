---
  - hosts: all
    become: yes
    become_method: sudo
    vars_files:
      - config_bambi.yml
      - static.yml

    roles:
      - base
      - role: wireguard_configs
        vars:
          config_dir: checkers
      - filebeat
      - journalbeat
      - metricbeat
      - vuln_checkers
      - docker-block-external
