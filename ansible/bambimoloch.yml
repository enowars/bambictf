---
  - hosts: all
    become: yes
    become_method: sudo
    vars_files:
      - config_bambi.yml
      - enomoloch-get-pcaps
    roles:
        - bambi-ssh-keys
        - firewall
        - enomoloch
        - role: filebeat
          vars:
            elk: 192.168.1.0
        - role: journalbeat
          vars:
            elk: 192.168.1.0
        - role: metricbeat
          vars:
            elk: 192.168.1.0